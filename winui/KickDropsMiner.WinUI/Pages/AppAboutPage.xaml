<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="KickDropsMiner_WinUI.Pages.AppAboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="28,24" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="30" FontWeight="SemiBold" Text="About" />
            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Kick Drop Miner WinUI fork information." />
        </StackPanel>

        <Border Grid.Row="1" Padding="18" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Width="48" Height="48" Source="/Assets/AppIcon.ico" />
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="Kick Drop Miner" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="WinUI 3 interface for Kick drops automation." />
                    <TextBlock x:Name="VersionText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Padding="18" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6" VerticalAlignment="Top">
            <StackPanel Spacing="10">
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Fork changes" />
                <TextBlock Text="Native WinUI 3 frontend" />
                <TextBlock Text="Multiple Kick accounts with per-drop account binding" />
                <TextBlock Text="Campaign-based drop queue with progress persistence" />
                <TextBlock Text="Cached Browse Drops loading with incremental row updates" />
                <TextBlock Text="Logging page with drop and creator filtering" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
