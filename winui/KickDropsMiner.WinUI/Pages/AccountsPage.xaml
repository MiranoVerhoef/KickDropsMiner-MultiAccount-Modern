<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="KickDropsMiner_WinUI.Pages.AccountsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="AccountTemplate">
            <Grid Padding="12,8" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding Name}" />
                <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding Id}" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{Binding CookieStatus}" />
                <Button Grid.Column="3" Click="LoginAccount_Click" Content="Login" Tag="{Binding Id}" />
                <Button Grid.Column="4" Click="RemoveAccount_Click" Content="Remove" Tag="{Binding Id}" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="28,24" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock FontSize="30" FontWeight="SemiBold" Text="Accounts" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Kick login cookies and account bindings." />
            </StackPanel>
            <Button Grid.Column="1" Click="AddAccount_Click" Content="Add account" Style="{StaticResource AccentButtonStyle}" />
        </Grid>

        <Grid Grid.Row="1" Padding="12,0" Height="38" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="Account" />
            <TextBlock Grid.Column="1" VerticalAlignment="Center" FontWeight="SemiBold" Text="Id" />
            <TextBlock Grid.Column="2" VerticalAlignment="Center" FontWeight="SemiBold" Text="Cookies" />
            <TextBlock Grid.Column="3" VerticalAlignment="Center" FontWeight="SemiBold" Text="Login" />
            <TextBlock Grid.Column="4" VerticalAlignment="Center" FontWeight="SemiBold" Text="Remove" />
        </Grid>

        <ListView
            x:Name="AccountList"
            Grid.Row="2"
            ItemTemplate="{StaticResource AccountTemplate}"
            ItemsSource="{x:Bind State.Accounts, Mode=OneWay}"
            SelectionMode="None" />
    </Grid>
</Page>
